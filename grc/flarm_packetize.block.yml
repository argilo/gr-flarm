# auto-generated by grc.converter

id: flarm_packetize
label: Packetize
category: '[FLARM]'

parameters:
-   id: num_channels
    label: Num Channels
    dtype: int
    default: '1'
    hide: part
-   id: first_channel
    label: 1st Channel
    dtype: int
    default: '32'
-   id: rxid
    label: Receiver ID
    dtype: string
    default: '00'
-   id: reflat
    label: Reference Latitude
    dtype: float
    default: '45.10513'
-   id: reflon
    label: Reference Longitude
    dtype: float
    default: '-75.623744'

inputs:
-   domain: stream
    dtype: byte
    multiplicity: ${ num_channels }

outputs:
-   domain: stream
    dtype: byte
asserts:
- ${ num_channels > 0 }
- ${ 0 <= first_channel }
- ${ first_channel + num_channels - 1 <= 64 }
- ${ -90 <= reflat <= 90 }
- ${ -180 <= reflon <= 180 }

templates:
    imports: import gnuradio.flarm as flarm
    make: flarm.packetize(${num_channels}, ${first_channel}, ${rxid}, ${reflat}, ${reflon})

file_format: 1
